{
  "name": "portFolio",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../servers && npm install",
    "start": "node servers/index.js"
}
,
  "engines": {
      "node": "18.x"
  },
  "dependencies": {
      "pdfjs-dist": "^3.0.279"
  }
}
